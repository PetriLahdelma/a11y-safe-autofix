# aria-label-from-title

Adds `aria-label` when a button-like element has a literal `title` but no `aria-label`.

Rationale: reusing a literal `title` string is deterministic and does not invent new text.
