# Constraints & Limitations

- Only fixes `<img>` tags missing `alt`.
- HTML/HTM only.
- Templates or JSX must be handled separately.

Performance notes: linear in HTML file count/size; regex scanning dominates.
